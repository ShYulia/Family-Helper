# Real secrets go in a local .env file, which is never committed.
# This file is a template — variables will be added here in later stages as they're needed.

NODE_ENV=
SUPERMARKET_NAME=
SUPERMARKET_URL=
